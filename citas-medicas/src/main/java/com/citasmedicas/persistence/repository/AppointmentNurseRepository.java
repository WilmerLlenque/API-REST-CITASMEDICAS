package com.citasmedicas.persistence.repository;

import com.citasmedicas.persistence.entity.AppointmentNurse;
import org.springframework.data.repository.ListCrudRepository;

public interface AppointmentNurseRepository extends ListCrudRepository<AppointmentNurse,Integer> {
}
